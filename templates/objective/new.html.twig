{% extends 'back/base.html.twig' %}

{% block title %}New Objective{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Create new Objective</h1>

        {{ include('objective/_form.html.twig') }}

        <a href="{{ path('objective_index') }}" class="btn btn-secondary mt-3">Back to list</a>
    </div>
{% endblock %}
